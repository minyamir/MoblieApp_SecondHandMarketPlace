class User {
  final String id;
  final String fullName;
  final String email;
  final bool isVerified;

  User({
    required this.id,
    required this.fullName,
    required this.email,
    required this.isVerified,
  });
}